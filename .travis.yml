sudo: required
language: java
jdk:
    - oraclejdk8
before_install:
  - mkdir .travis-github
  - git clone https://github.com/toutfic/travis.git .travis-github
  - chmod +x .travis/deploy.sh
script:
  - mvn clean install -DskipTests
  - .travis/deploy.sh
