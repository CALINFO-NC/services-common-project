common:
  configuration: # Configuration du common

    tenant: # Propriété de la classe TenantProperties

    security: # Propriété de la classe SecurityProperties

    application: # Propriété de la classe ApplicationProperties

    kafka-event: # Propriété de la classe KafkaProperties

    deprecated:
      swagger: true/false # Prise en charge du swagger malgré qu'il soit déprécié
